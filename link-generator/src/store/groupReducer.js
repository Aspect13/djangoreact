const linkGroupsState = {
    list: [],
    selectedGroup: null
};

export default function groupReducer(state = linkGroupsState, action) {
    switch (action.type) {
        default:
            return state;
    }
};

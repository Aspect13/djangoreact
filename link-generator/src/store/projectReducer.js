const projectState = {
    list: [],
    selectedProject: null
};

export default function projectReducer(state = projectState, action) {
    switch (action.type) {
        default:
            return state;
    }
};
